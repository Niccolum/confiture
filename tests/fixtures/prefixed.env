# Configuration with prefix APP_
APP_NAME=PrefixedApp
APP_ENVIRONMENT=production

# These should be filtered out (no APP_ prefix)
OTHER_VALUE=ignored
RANDOM_KEY=not_loaded

# Nested with prefix
APP_DATABASE__HOST=prod.db.com
APP_DATABASE__PORT=5432
